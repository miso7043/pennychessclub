import { mainStyle } from '../../styles/mainStyle';

export default function PCCOpen5() {
  return (
    <div className={mainStyle.pageContainer}>
      <h1 className={mainStyle.h1.tournament}>PCC OPEN 5 (May 25th)</h1>
      
      <section className={mainStyle.section}>
        <h2 className={mainStyle.h2}>Tournament Information</h2>
        <p className={mainStyle.text.body}>Details coming soon...</p>
      </section>
    </div>
  );
}
